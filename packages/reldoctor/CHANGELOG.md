# Changelog

## Unreleased

## [1.0.0] - 2026-08-15

- 首版：发布卫生体检 CLI
- 检测 version / 最新 git tag / CHANGELOG 三者一致性（7 条规则）
- 0–100 健康分 + `--fail-on-high` / `--max-*` / `--min-score` / `--json` CI 门禁
- 零依赖单文件，仅 Node 内置模块 + 系统 git，不联网、不执行用户代码
